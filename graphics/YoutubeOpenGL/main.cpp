#include <GLFW/glfw3.h>
#include <iostream>

// Entry Point for program
int main() {
  // Create Window

  // Failed to create window
  if (window == NULL) {
    glfwTerminate();
    return -1;
  }

  // Exit Program
  glfwTerminate();
  return 0;
}
